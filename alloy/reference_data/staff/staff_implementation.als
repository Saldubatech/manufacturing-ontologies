module reference_data/staff/staff_implementation

/*
 * STAFF — IMPLEMENTATION (DT-017, the STATIC DEGENERATE form — the business_affiliate
 * precedent): no machinery beyond the types, so the implementation IS the contract
 * stated as fact (impl ≡ mock; the suite verifies joint satisfiability).
 */

open reference_data/staff/staff_types
open reference_data/staff/staff_contracts

fact StaffLaws { guarantees }
