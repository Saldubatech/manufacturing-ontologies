module meta/std/bfo

/*
 * STUB — vendored BFO boundary stubs (MIREOT). Only the BFO terms our entities
 * actually touch (e.g. object, site, role, process), each tagged with its
 * source IRI. Empty for now; populated per DT-002. The public BFO standard (cached
 * read-only under ../owl/imports/) stays source-of-truth; departures are documented.
 */
