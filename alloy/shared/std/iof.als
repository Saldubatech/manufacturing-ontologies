module shared/std/iof

/*
 * STUB — vendored IOF Core boundary stubs (MIREOT). Only the IOF terms our
 * entities touch (e.g. MaterialResource, Agent, EngineeredSystem, the ICE
 * family), each tagged with its source IRI. Empty for now; populated per DT-002.
 */
